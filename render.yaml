# render.yaml

services:
  - type: web
    name: votingsphere-api
    env: python
    plan: free
    autoDeploy: true

    # Build: install deps + collect static
    buildCommand: |
      export DJANGO_SETTINGS_MODULE=VotingSphere.settings.prod
      pip install -r requirements.txt
      python manage.py collectstatic --noinput

    # Free plan: run migrations at startup, then start Daphne
    startCommand: |
      export DJANGO_SETTINGS_MODULE=VotingSphere.settings.prod
      python manage.py migrate --noinput && \
      daphne -b 0.0.0.0 -p $PORT VotingSphere.asgi:application

    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
      - key: DJANGO_SETTINGS_MODULE
        value: VotingSphere.settings.prod
      - key: SECRET_KEY
        generateValue: true

      # Postgres from managed DB below
      - key: DATABASE_URL
        fromDatabase:
          name: votingsphere-pg
          property: connectionString

      # Redis URL from managed Redis service below
      - key: REDIS_URL
        fromService:
          type: redis
          name: votingsphere-redis
          property: connectionString

      # First deploy: keep wide open; tighten to your Render host afterwards
      - key: ALLOWED_HOSTS
        value: "*"
      - key: CORS_ALLOWED_ORIGINS
        value: "http://localhost:5173,http://127.0.0.1:5173"
      - key: CSRF_TRUSTED_ORIGINS
        value: "http://localhost:5173,http://127.0.0.1:5173"

      # JWT lifetimes
      - key: JWT_ACCESS_MINUTES
        value: "30"
      - key: JWT_REFRESH_DAYS
        value: "10"

  # Managed Redis (free)
  - type: redis
    name: votingsphere-redis
    plan: free

databases:
  - name: votingsphere-pg
    plan: free
